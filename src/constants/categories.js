/** Dock order: Background, Text, Button, Online Inspirations */
export const DEFAULT_CATEGORIES = ['Background effect', 'Text effect', 'Button interactions', 'all']

/** Category shown when opening the app with no ?category= in the URL */
export const DEFAULT_CATEGORY = 'Background effect'

/** Titles that belong in "Online Inspirations" (all) — only these show as tiles there. */
export const ONLINE_INSPIRATION_TITLES = ['Avatar Group', 'Cursor']
